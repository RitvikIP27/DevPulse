# Test-only dependencies. Kept separate from requirements.txt so the runtime
# image does not carry test tooling (see backend/.dockerignore).
-r requirements.txt

pytest==8.3.3
respx==0.21.1      # records/replays httpx traffic so connector tests need no network
