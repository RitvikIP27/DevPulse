[pytest]
testpaths = tests
pythonpath = .
# Behaviour-oriented test names are long by design (testing.md 24); show them in full.
addopts = -v --strict-markers --tb=short
markers =
    known_defect: locks in a defect the audit proved, so the fixing stage flips it to pass.
